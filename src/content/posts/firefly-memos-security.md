---
title: Firefly 魔改：为 Memos 动态建立内容安全边界
published: 2026-09-07
description: 从远端 Markdown 转义、链接协议、同源图片和请求限制入手，记录 Firefly Memos 适配器的安全改造与验收边界。
image: ""
tags: [Firefly, Memos, 安全]
category: Firefly
slug: firefly-memos-security
draft: true
---

Memos 内容来自外部服务，即使实例属于自己，也不应该把返回的文本直接当作可信 HTML。Firefly 的动态页和侧栏共用同一套适配器，因此安全边界应放在 `src/utils/memos-adapter.ts`，而不是分别在两个组件里补丁式处理。

本文记录的是启用前的安全改造。`src/config/dynamicConfig.ts` 中没有打开 `memos.enable`，当前网站继续使用本地动态；在真实实例和浏览器回归完成前，本文保持草稿状态，不作为上线启用指南。

## 一、先把远端内容变成受控输出

详情页需要把动态内容放进 `innerHTML`，所以不能把 Memos 的 Markdown 或 HTML 原样交给浏览器。`markdownToHtml` 采用简单的固定转换流程：

1. 先移除 Markdown 图片语法，图片交给独立的来源校验流程处理；
2. 单独检查 Markdown 链接的目标，只允许 `http:`、`https:`、`mailto:` 和受控相对地址，并拒绝会被浏览器规范化为外站地址的反斜杠变体；
3. 把其余文本完整 HTML 转义，使 `<script>`、事件属性、内联样式和 iframe 只能作为普通文字出现；
4. 最后只生成标题、强调、代码、列表、引用、分隔线和链接等固定标签。

这样做的重点不是“猜测哪些 HTML 标签安全”，而是远端内容不负责生成 HTML 标签，标签只能由本地代码按白名单产生。

## 二、图片要单独限制来源

图片不是普通文字。`extractImages` 会重新解析 Markdown 图片和 Memos 附件，只接受 HTTPS、与配置的 Memos 实例同源且在实例路径范围内的 URL，同时拒绝凭据、控制字符和路径穿越。附件 ID 与文件名会经过 URL 编码。

这意味着外站图片、HTTP 图片或异常附件会被主动丢弃。旧动态如果依赖外部图床，需要在启用前先核对；安全策略不能为了“尽量显示图片”而放宽到任意外部地址。

## 三、限制请求和响应规模

`fetchMemos` 对实例地址、分页、条目数、响应体、字段长度、附件数量和请求时间都设有上限：默认超时 3 秒，最多 10 秒；默认最多 5 页、300 条动态；单次响应体最多 1 MiB。调用方取消请求时，适配器会传递取消信号；没有调用方信号的并发调用会共享进行中的请求。

请求使用 `credentials: "omit"`、`redirect: "error"` 和 `referrerPolicy: "no-referrer"`。非 2xx 响应只记录状态码，不把远端错误正文写入浏览器控制台，避免把实例内部路径或其他敏感信息暴露给访客。

## 四、当前配置仍然关闭

这次改造不会自动启用 Memos，也不需要令牌。当前保持本地动态：

```ts
// src/config/dynamicConfig.ts
// 未配置 memos，继续使用 /api/dynamic.json
```

未来评估启用时，至少要先确认真实实例的 HTTPS 地址、Memos 版本、CORS 行为、`parent` 过滤语义、动态页和侧栏渲染，以及超时/错误/Swup 切换后的清理行为。令牌不能放在客户端。

## 五、验证结果与边界

2026 年 9 月 7 日，对抗式审查补充了反斜杠协议相对链接用例；Memos 适配器专项测试 **22/22 通过**，覆盖了 Markdown 转义、危险链接、同源图片、路径穿越、响应结构、公开状态过滤、分页和规模限制、超时、调用方取消，以及非 2xx 日志不泄露响应正文。`pnpm type-check` 通过，`pnpm check` 也通过（217 个文件，0 errors、0 warnings、0 hints）。

这些证据证明当前安全边界在本地代码层面成立，不等于任何 Memos 实例都已经兼容。项目目前没有可用于验收的真实实例，因此浏览器端 Memos 仍未上线，本文也继续保持 `draft: true`。

## 六、性能与回滚

没有新增运行时依赖，也没有构建期远端请求。Memos 关闭时不会新增请求；即使未来开启，请求去重、分页上限、响应体上限和图片数量上限也会控制额外开销。当前只做进行中的请求去重，没有贸然加入成功缓存，以免在没有明确新鲜度需求时显示过期动态。

遇到问题时，保持或恢复 `memos.enable: false`，继续使用 `/api/dynamic.json`。不要为了恢复页面而重新启用未经清洗的远端 HTML，也不要把访问令牌写入前端配置。
